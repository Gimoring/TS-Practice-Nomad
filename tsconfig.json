{
	"compilerOptions": {
		"module": "CommonJS",
		"target": "ES2015", // what version of javascript we want to compile to
		"sourceMap": true,
		"outDir": "dist",
		"rootDir": "src"
	},
	"include": ["src/**/*"],
	"exclude": ["node_modules"] // it's good to exclude it as default
}
